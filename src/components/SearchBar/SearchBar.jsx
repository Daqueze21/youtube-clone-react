import React from 'react';
import styles from './SearchBar.module.scss';

const SearchBar = () => {
   return <div className={styles.container}>SearchBar component</div>;
}

export default SearchBar;
